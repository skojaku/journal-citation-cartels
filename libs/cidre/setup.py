#!/usr/bin/env python
# encoding: utf-8
from setuptools import setup, find_packages

setup(name='cidre',
      version='0.0.1',
      description = 'CIDRE',
      author = 'Sadamori Kojaku',
      packages=find_packages(),
      zip_safe=False)
